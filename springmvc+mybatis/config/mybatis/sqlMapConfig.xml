<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>


    <!--全局setting配置，根据需要再加-->

    <!--配置mapper-->
    <!--由于使用了spring和mybatis的整合包进行mapper扫描，这里不需要配置了，
    必须遵循一些规范：mapper.java与mapper.xml文件同名且在一个目录-->

    <!--配置别名-->
    <typeAliases>
        <!--批量扫描po包的别名-->
        <package name="com.tingfeng.ssm.po"/>
    </typeAliases>
</configuration>
